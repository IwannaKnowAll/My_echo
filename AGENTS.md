# 【AGENT】本地备忘录 App（My Echo）— 主 Agent 协调规范

> 本文件是协作流程规范与需求细节的唯一事实源，所有角色 Agent 开工前必读。
> 与 Planner 规划文档、UI_Designer 设计文档冲突时，以本文件为准。
> 需求变更只改本文件，禁止在 agents/*.toml 中重复定义需求细节。

## 1. 任务分发原则
- 需求分析、SQLite 表结构、Tauri Command 契约、任务拆解 → 必须 spawn Planner 子 Agent
- UI/UX 设计 → 必须 spawn UI_Designer 子 Agent
- 前端代码改动（Vue3 组件、TypeScript 类型、样式）→ 必须 spawn Frontend 子 Agent，主 Agent 不得直接修改 client/ 下代码
- 壳层代码改动（Command 实现、SQLite 存取、打包配置）→ 必须 spawn Shell 子 Agent，主 Agent 不得直接修改 src-tauri/ 下代码
- 质量审查与测试 → 必须 spawn Reviewer 子 Agent，Reviewer 只可写测试文件（前端测试在 client/src 内的 __tests__ 目录，壳层测试在 src-tauri/src 内的 tests 目录），禁止修改业务代码
- 主 Agent 的职责：协调流程、集成验证、修复配置文件、修复不归属任何子 Agent 的问题
- 违反此原则视为流程违规

## 2. 质量门禁与自动修复循环
### 2.1 循环流程
Planner（出规划）→ UI_Designer（出设计）
→ Frontend + Shell（并行开发）
→ Reviewer（质量审查：契约一致性、设计一致性、代码质量、测试覆盖、Unicode 检查）
→ 如果存在 P1/P2 问题
 → 对应子 Agent 修复（Frontend 修前端，Shell 修壳层）
 → Reviewer 增量复审（修复方必须注明修改的文件列表）
 → 重复直到 P1 清零、P2 ≤ 2 或达到最大循环次数
→ 通过后进入集成验证
→ 集成验证（主 Agent 执行）
 → 启动应用、检查 SQLite 读写、走通核心流程
 → 如果发现集成问题 → 回退给对应子 Agent 修复 → 回到 Reviewer
→ 通过后由 Shell 打包发布，Reviewer 查验发布清单
→ 结束

### 2.2 回退规则
- **P1问题**：必须修复，阻塞后续流程
- **P2问题**：建议修复，同一模块循环上限 3 次后自动降级为建议项
- **P3问题**：记录到待办清单，不阻塞当前循环
- 子 Agent 修复时只修改自己负责的目录（Frontend 只改 client/，Shell 只改 src-tauri/）
- 修复完成后必须注明修改的文件列表，方便 Reviewer 增量审查

### 2.3 集成验证
代码审查通过后，由主 Agent 执行集成验证，检查以下内容：
- 应用启动是否正常（编译、运行报错）
- SQLite 初始化是否正常（建表、连接、增删改查）
- 核心流程是否可用（备忘录新建、编辑、删除、搜索）
- 打包配置是否正确（版本号、代码签名配置）
- 运行环境是否兼容（macOS 版本适配）

集成验证发现的问题按同等级别回退到修复循环中处理。

### 2.4 人工介入条件
- 同一问题反复出现 3 次仍未解决 → 标记为需人工介入，跳出循环
- 跨模块/跨目录的架构问题 → 通知主 Agent 人工决策
- 集成验证发现的环境/配置问题，子 Agent 无法独立解决 → 主 Agent 人工处理

## 3. 编码规范（所有子 Agent 必须遵守）
### 3.1 中文字符编码
- 所有源码文件（.vue、.ts、.js、.css、.toml 等）中的中文文本**必须保存为实际 UTF-8 中文字符**，禁止使用 Unicode 转义序列（\uXXXX）
- 写入文件时，必须确保非 ASCII 字符原样保持，不被转义为 \uXXXX 序列
- 所有文件必须使用 UTF-8 编码，不含 BOM

### 3.2 文本用于界面展示
模板和组件中的中文文本（label、placeholder、message 等）必须是可读的中文字符，而非编码后的转义序列。示例：
```html
<!-- 正确 -->
<input placeholder="请输入备忘录标题" />

<!-- 错误 -->
<input placeholder="\u8bf7\u8f93\u5165\u5907\u5fd8\u5f55\u6807\u9898" />
```

### 3.3 类型安全
- 禁止 any，严格定义 TS Interface
- Command 入参与返回值必须与 Planner 契约一致

## 4. 硬约束（不可违背）

1. 技术栈：Tauri + Vue3 CompositionAPI + TypeScript + SQLite 本地存储。
2. 无 HTTP 后端服务、无 MySQL、无登录、无云端同步。
3. 数据全部保存在 Mac 本机 SQLite，禁止发起网络请求。
4. 运行约束遵守根目录 config.toml（审批策略、沙箱模式、推理强度）。
5. 目录边界：按本文件第 1 节任务分发原则执行，禁止生成后端服务代码。

## 5. 需求事实源

### 5.1 项目概述
- 目标：Mac 桌面本地备忘录应用，纯本地存储，无网络依赖。
- 用户：单机单用户，面向个人日常记录。

### 5.2 核心功能清单
> 优先级规则：P0 首版必做，P1 次版，P2 远期。每项必须有可验收的标准，Reviewer 据此检查。

| 编号 | 功能 | 描述 | 优先级 | 验收标准 |
|---|---|---|---|---|
| F01 | 备忘录增删改查 | 新建、查看、编辑、删除备忘录 | P0 | CRUD 四操作均生效；应用重启后数据不丢失 |
| F02 | 搜索与排序 | 按标题+内容模糊搜索；列表按更新时间倒序 | P0 | 输入关键词能过滤出匹配备忘录；列表最新在前 |
| F03 | 置顶与归档 | 置顶备忘录固定显示在列表顶部；归档后移入归档区 | P1 | 置顶项始终排最前；归档项不出现在默认列表，可在归档区查看 |
| F04 | 回收站 | 删除改为软删，可恢复 | P1 | 删除后可从回收站恢复；回收站为空时无残留数据 |
| F05 | 本地加密与备份 | 正文字段加密存 SQLite；支持手动导出/导入备份 | P2 | 重启后数据正常解密读取；导出导入完整往返 |

### 5.3 数据字段定义
> 本表是 Planner 产出建表 SQL 与 Command 契约的唯一依据，字段变更必须先改这里。
> 支持多实体时，每个实体单独一张表。

备忘录实体：

| 字段 | 类型 | 必填 | 默认值 | 约束 | 说明 |
|---|---|---|---|---|---|
| id | INTEGER | 是 | 自增 | 主键 | 唯一标识 |
| title | TEXT | 是 | 空字符串 | 长度 ≤ 100 | 标题 |
| content | TEXT | 否 | 空字符串 | 无 | 正文 |
| created_at | TEXT | 是 | 当前时间 | ISO8601 格式 | 创建时间 |
| updated_at | TEXT | 是 | 当前时间 | ISO8601 格式 | 更新时间 |

### 5.4 业务规则
- 校验规则：标题必填，空标题不可保存；正文允许为空（仅标题速记场景）
- 排序规则：列表按 updated_at 倒序
- 搜索规则：标题 + 正文模糊匹配（LIKE），大小写不敏感
- 数据保留：P0 硬删，删除前弹确认框；P1 升级为软删回收站，30 天后自动清理

### 5.5 术语表
> 统一概念命名，保证各角色描述一致。例如「备忘录」「标签」「归档」的定义。

| 术语 | 定义 |
|---|---|
| 备忘录 | 一条记事数据，由标题和正文组成 |
| 置顶 | 备忘录固定显示在列表最前，不受时间排序影响 |
| 归档 | 备忘录移出默认列表，单独存放于归档区 |
| 回收站 | 软删数据暂存区，可恢复，暂存 30 天后自动清理 |

## 6. 变更约定

- 需求变更 → 更新本文件 → Planner 重新输出契约 → UI_Designer 更新设计 → Frontend/Shell 按新契约执行 → Reviewer 按新契约审查。